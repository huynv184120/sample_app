regex:
  VALID_EMAIL_REGEX : !ruby/regexp '/\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i'
valid:
  password:
    minlength: 6
  email:
    minlength: 10
    maxlength: 50
  name: 
    maxlength: 50
pagy:
  page_size: 10
avatar:
  default_size: 80
  size: 50
